# 작업 진행 요약

**작성일**: 2025-12-11
**최종 업데이트**: 2025-12-11 (UI/UX 개선 계획 수립)

---

## 1. 현재까지의 작업 결과

### Phase 1: 개발 기반 작업 ✓
- 프로젝트 디렉터리 구조 재구성 (archive/, docs/, lib/, tests/)
- Python 가상환경 및 requirements.txt 업데이트
- pyproject.toml (ruff 포맷터 설정)
- GitHub Actions CI 워크플로우 (.github/workflows/ci.yml)
- .env.example, .gitignore 업데이트

### Phase 2: Python 확장 ✓
- **lib/**: 공통 모듈 생성
  - `lib/db.py`: SQLite 연결 공통화
  - `lib/telegram.py`: Telethon 클라이언트 공통화
  - `lib/supabase.py`: Supabase 연결 모듈
- **chat_list.py**: 채팅방 목록 조회 CLI (JSON 출력)
- **searcher.py**: --json, --chat-id 플래그 추가
- **sync.py**: Supabase 동기화 (1,000개 배치 UPSERT)
- **tests/**: 단위 테스트 작성 완료

### Phase 3: Supabase 설정 ✓
- pg_trgm 확장 활성화
- messages 테이블 생성 (id, chat_id, sender_id, date, text, synced_at)
- GIN 인덱스 생성 (text gin_trgm_ops)
- 복합 인덱스 생성 (chat_id, date DESC)
- RLS 정책 설정 (anon: SELECT, service_role: ALL)
- 동기화 테스트 완료: **365건 메시지 업로드 성공**

### Phase 4: Tauri Desktop 앱 ✓
- **4.1 Tauri 프로젝트 초기화** ✓
  - Tauri 2.0 + React + TypeScript 프로젝트 생성
  - tauri.conf.json 설정 (TeleSearch-KR)
- **4.2 Rust Backend** ✓
  - commands/chat_list.rs: get_chat_list 커맨드
  - commands/indexing.rs: start_indexing, is_indexing, cancel_indexing, run_sync, is_syncing, cancel_sync 커맨드
  - commands/search.rs: run_search 커맨드
  - `get_project_root()` 함수로 프로젝트 루트 경로 탐색 로직 구현
  - **[NEW]** lazy_static으로 프로세스 관리 (취소 기능 지원)
  - **[NEW]** JSON 기반 실시간 Progress 스트리밍
- **4.3 React Frontend** ✓
  - hooks/useTauriCommand.ts, useSearch.ts
  - components: ChatSelector, IndexingPanel, SearchBar, SearchOptions, ResultList
  - App.tsx, App.css (다크 테마)
  - **[NEW]** Progress UI 컴포넌트 (진행률, 예상시간, 취소버튼)
- **4.4 Desktop 통합 테스트** - 테스트 필요

---

## 2. 지금까지의 주요 결정 사항

- 기존 CLI 코드(indexer.py, searcher.py)는 변경 없이 유지
- 문서들을 docs/ 폴더로 이동, 기존 버전은 archive/에 보관
- Supabase PostgreSQL + pg_trgm으로 클라우드 검색 지원
- 로컬 SQLite가 원천(Source), Supabase는 캐시(Cache) 역할
- Tauri 2.0 + React + TypeScript로 데스크톱 앱 구현
- Python 스크립트는 subprocess로 호출 (기존 로직 재사용)
- **[NEW]** Progress/Cancel 기능: Python stdout을 JSON 스트리밍으로 변경, Rust에서 실시간 파싱

---

## 3. 알려진 오류 및 수정 완료 사항

### 3.1 Python 스크립트 경로 문제 ✓
- **증상**: chat_list.py를 `desktop/chat_list.py`에서 찾으려 함
- **원인**: Tauri dev 모드에서 `current_dir()`이 예상과 다르게 동작
- **해결**: `get_project_root()` 함수 구현

### 3.2 chat_list.py 인자 오류 ✓
- **증상**: "unrecognized arguments: --json"
- **원인**: Rust에서 `--json`으로 호출, Python은 `--format json` 기대
- **해결**: chat_list.rs에서 `.arg("--format").arg("json")`으로 수정

### 3.3 SQLite 데이터베이스 잠금 ✓
- **증상**: "database is locked"
- **원인**: 다른 Python 프로세스가 session 파일 점유
- **해결**: 해당 프로세스 종료

### 3.4 Takeout 세션 충돌 ✓
- **증상**: "Can't send a takeout request while another takeout..."
- **원인**: 이전 takeout 세션이 정상 종료되지 않음
- **해결**: `client.end_takeout(success=False)` 호출

### 3.5 남은 테스트 항목
- [ ] 채팅방 목록 조회 → UI 표시 테스트
- [ ] 인덱싱 Progress 표시 테스트
- [ ] 인덱싱 Cancel/Rollback 테스트
- [ ] Supabase 동기화 Progress 표시 테스트
- [ ] Supabase 동기화 Cancel/Rollback 테스트
- [ ] 검색 실행 → 결과 표시 테스트
- [ ] 결과 클릭 → 브라우저 링크 열기 테스트

---

## 4. 이번 세션에서 구현된 4가지 기능

### 4.1 실시간 Progress 표시
- Python 스크립트에 `--json-progress` 플래그 추가
- stdout으로 JSON 형식 진행상황 출력
- Rust에서 비동기 스트리밍으로 파싱 후 Tauri Event emit
- React에서 `listen()` API로 실시간 UI 업데이트

### 4.2 예상시간/진행률 표시
- `elapsed_sec`, `eta_sec`, `percentage`, `rate` 필드 추가
- 작업 시작 시간 기록, 진행률 기반 ETA 계산
- UI에서 "경과: 3분 20초", "남은 시간: ~5분", "42%", "15.3 msg/s" 표시

### 4.3 인덱싱 취소 + Rollback
- SIGINT/SIGTERM 시그널 핸들러 등록
- 취소 시 Telegram takeout 세션 종료 (`end_takeout`)
- 현재 세션에서 수집된 메시지 ID 추적
- 취소 시 해당 메시지들 SQLite에서 삭제 (롤백)

### 4.4 Supabase 동기화 취소 + Rollback
- 동기화된 메시지 ID 목록 추적
- 취소 시 Supabase에서 해당 메시지들 DELETE
- 배치 단위 삭제로 대량 데이터 안전 처리

---

## 5. 현재 중단된 지점

- **UI/UX 개선 계획 수립 완료** (구현 대기)
- Progress/Cancel 기능 구현 완료
- Tauri dev 서버 실행 중
- 실제 동작 테스트 필요

---

## 6. 다음 작업: UI/UX 개선 (계획 완료, 미구현)

### 6.1 요청된 기능 (6가지)
1. **정렬 옵션**: 최신순/과거순 선택
2. **페이지네이션**: 20개씩, 50개씩, 100개씩 끊어서 보여주기
3. **결과 카운트**: "1-20 of 325개" 형식
4. **컴팩트 레이아웃**: 링크를 시간 옆으로 이동
5. **여백 최소화**: 한 화면에 더 많은 결과 표시
6. **2컬럼 레이아웃**: 좌측 사이드바(200px) + 우측 메인(검색/결과)

### 6.2 계획된 레이아웃
```
┌────────────────────┬──────────────────────────────────────┐
│ [채팅방▼] [🔄]     │ 검색:[_______________] [검색]        │
│ [인덱싱] [동기화]  │ 정렬:[최신▼] 페이지당:[20▼]         │
│ ████░░ 80%         ├──────────────────────────────────────┤
│ 경과:2분|ETA:30초  │ 1-20 of 325 (23ms)   [<이전][다음>] │
├────────────────────┼──────────────────────────────────────┤
│                    │ [1] 24-03-15 14:32           [🔗]   │
│                    │ 텔레그램에서 재밌는...               │
│                    │──────────────────────────────────────│
│                    │ [2] 24-03-10 09:15           [🔗]   │
│                    │ 텔레그램 봇 만들기...                │
└────────────────────┴──────────────────────────────────────┘
```

### 6.3 수정 예정 파일 (7개)
| 파일 | 변경 내용 |
|------|----------|
| `searcher.py` | --offset, --sort 인자 추가, total 반환 |
| `search.rs` | offset, sortOrder 파라미터 전달 |
| `useSearch.ts` | 페이지네이션 상태 관리 |
| `SearchOptions.tsx` | 정렬/페이지당 선택 UI |
| `ResultList.tsx` | 컴팩트 레이아웃 + 페이지네이션 |
| `App.tsx` | 2컬럼 레이아웃 |
| `App.css` | grid + 컴팩트 스타일 |

### 6.4 취소된 기능
- ~~2글자 검색~~: trigram 특성상 성능 저하 우려로 취소, 최소 3글자 유지

### 6.5 계획 파일 위치
- `.claude/plans/snazzy-sauteeing-thimble.md`

---

## 7. 다음 작업을 시작하기 위한 힌트

1. **UI/UX 개선 구현** (계획 승인 후):
   - 계획 파일: `.claude/plans/snazzy-sauteeing-thimble.md`
   - "이 계획으로 진행해" 또는 "시행해"로 시작

2. **Phase 4.4 테스트 진행**:
   ```bash
   cd ~/git/telegram_advanced_search/desktop
   pnpm tauri dev
   ```
   - 채팅방 목록 로드 확인
   - 인덱싱 시작 → Progress UI 확인
   - 인덱싱 중 취소 → Rollback 확인
   - Supabase 동기화 → Progress UI 확인
   - 검색 → 결과 표시 확인

3. **Phase 5 (Web) 진행 시**:
   ```bash
   cd ~/git/telegram_advanced_search/web
   pnpm create next-app . --typescript --tailwind
   ```

4. **참고 파일**:
   - [docs/PRD.md](docs/PRD.md): 기능 요구사항
   - [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md): 시스템 구조
   - [docs/WBS.md](docs/WBS.md): 작업 분해

---

## 8. Phase 4 구현 결과물

### desktop/ 디렉터리 구조
```
desktop/
├── src/                          # React Frontend
│   ├── components/
│   │   ├── ChatSelector.tsx      # 채팅방 드롭다운
│   │   ├── IndexingPanel.tsx     # 인덱싱 UI + Progress/Cancel
│   │   ├── SearchBar.tsx         # 검색 입력
│   │   ├── SearchOptions.tsx     # 검색 옵션
│   │   └── ResultList.tsx        # 검색 결과
│   ├── hooks/
│   │   ├── useTauriCommand.ts    # IPC 래퍼
│   │   └── useSearch.ts          # 검색 상태 관리
│   ├── App.tsx
│   ├── App.css                   # Progress UI 스타일 추가
│   └── main.tsx
├── src-tauri/
│   └── src/
│       ├── commands/
│       │   ├── mod.rs
│       │   ├── chat_list.rs      # get_chat_list + get_project_root
│       │   ├── indexing.rs       # start_indexing, is_indexing, cancel_indexing, run_sync, is_syncing, cancel_sync
│       │   └── search.rs         # run_search
│       ├── lib.rs
│       └── main.rs
├── package.json
├── Cargo.toml                    # lazy_static 의존성 추가
└── tauri.conf.json
```

### 주요 수정 사항 (2025-12-11)
- `get_project_root()` 함수 추가: 개발/프로덕션 모드 모두 지원하는 경로 탐색 로직
- search.rs, indexing.rs에서 공통 함수 사용하도록 리팩토링

### 주요 수정 사항 (2025-12-11 - Progress/Cancel 기능)

#### Python 스크립트 수정
- **indexer.py**: `--json-progress` 플래그, 시그널 핸들러, 롤백 기능
- **sync.py**: `--json-progress` 플래그, 롤백 기능

#### Rust Backend 수정
- **indexing.rs**:
  - `IndexingProgress`, `SyncProgress` 구조체 확장 (percentage, eta_sec, rate, rolled_back)
  - `lazy_static`으로 `INDEXING_CHILD`, `SYNC_CHILD` 프로세스 관리
  - `cancel_indexing()`, `cancel_sync()` 커맨드 추가
  - stdout 스트리밍으로 실시간 JSON 파싱

#### React Frontend 수정
- **IndexingPanel.tsx**:
  - `indexing-progress`, `sync-progress` 이벤트 리스너
  - Progress bar UI (진행률, 경과시간, 남은시간, 속도)
  - Cancel 버튼 및 롤백 결과 표시
- **App.css**: Progress 관련 스타일 추가
